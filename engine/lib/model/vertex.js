class Vertex {
  constructor (coordinates, normal, world, textureCoordinates) {
    this.coordinates = coordinates
    this.normal = normal
    this.worldCoordinates = world
    this.textureCoordinates = textureCoordinates
  }
}

export default Vertex
